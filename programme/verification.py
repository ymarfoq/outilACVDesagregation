def verification(sigma_correlated, sigma_nocorrelated):
	from numpy import loadtxt, log, sqrt, var, mean, transpose, matrix
	
	print sigma_correlated[:,end]
	print sigma_nocorrelated[:,end]
	
	
	return transpose(matrix(sigma))
